# download
$ brew install postgres

# run
$ nohup /usr/local/bin/postgres -D /usr/local/var/postgres > postgres.log 2>&1 &

# admin
# database: postgres
# username: `whoami`
# password: blank

$ createuser silky-app
$ createdb --owner=silky-app --encoding=UTF8 silky-db

# interactive console
$ psql -U silky-app -W silky-db
